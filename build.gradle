plugins {
    id 'java-library'
    id 'idea'
    id 'maven-publish'
    id 'net.neoforged.gradle.userdev' version '7.0.145'
}

version = project.mod_version
group = 'com.github.aeddddd'

base {
    archivesName = 'AE2Enhanced'
}

java.toolchain.languageVersion = JavaLanguageVersion.of(project.java_version.toInteger())

// runs 配置暂时注释：NeoGradle 7.x 需要通过网络解析 net.neoforged:neoforge 才能生成 run types。
// 当前使用本地 universal jar，runClient/runServer 待网络可用或本地 Maven 仓库补齐后恢复。
// runs {
//     configureEach {
//         workingDirectory project.file('run')
//         modSource project.sourceSets.main
//     }
//     client {
//         systemProperty 'forge.enabledGameTestNamespaces', project.mod_id
//     }
//     server {
//         systemProperty 'forge.enabledGameTestNamespaces', project.mod_id
//     }
//     data {
//         programArgument '--mod'
//         programArgument project.mod_id
//         programArgument '--all'
//         programArgument '--output'
//         programArgument file('src/generated/resources/').getAbsolutePath()
//         programArgument '--existing'
//         programArgument file('src/main/resources/').getAbsolutePath()
//     }
// }

sourceSets.main.resources { srcDir 'src/generated/resources' }

repositories {
    mavenCentral()
    maven { url = 'https://www.cursemaven.com' }
    maven { url = 'https://modmaven.dev/' }
    maven { url = 'https://maven.blamejared.com' }
}

dependencies {
    // 本地 NeoForge/Forge 1.20.1 相关 jar（由用户本地 Prism Launcher 库提供）
    implementation fileTree(dir: 'libs', include: '*.jar')
}

tasks.withType(ProcessResources).configureEach {
    var replaceProperties = [
            version: version,
            mod_id: project.mod_id,
            mod_name: project.mod_name
    ]
    inputs.properties replaceProperties

    filesMatching(['META-INF/mods.toml']) {
        expand replaceProperties
    }
}

tasks.withType(JavaCompile).configureEach {
    options.encoding = 'UTF-8'
}

idea {
    module {
        downloadSources = true
        downloadJavadoc = true
    }
}
